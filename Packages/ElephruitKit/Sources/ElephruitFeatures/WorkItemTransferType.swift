import UniformTypeIdentifiers

extension UTType {
    /// A private drag type for moving work inside the app.
    static let elephruitWorkItemDrag = UTType(exportedAs: "com.elephruit.work-item-drag")
}
