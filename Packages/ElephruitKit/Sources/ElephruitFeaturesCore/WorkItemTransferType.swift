import UniformTypeIdentifiers

extension UTType {
    /// A private drag type for moving work inside the app.
    public static let elephruitWorkItemDrag = UTType(exportedAs: "com.elephruit.work-item-drag")
}
