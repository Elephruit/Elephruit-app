# Elephruit web spike

A focused web companion for logging interactions with people, noting relationships,
keeping facts, and tracking basic follow-ups. Runs against local Firebase emulators;
see the repo's docs/38 scope document once it lands.

Setup and run instructions are filled in as the spike is built.
