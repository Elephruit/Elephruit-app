# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files stay out even if this directory is ever extracted from the
# repo whose root .gitignore currently also covers them.
.env
.env.*
!.env.example
!.env.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# A local emulator port map, for running a second suite beside another worktree.
firebase.local.json
