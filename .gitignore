# Xcode
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.moved-aside

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store

# Per-worktree Xcode state — see Scripts/xctest.sh
.xcode-local/

# Web spike (web/) — Node toolchain and Firebase emulator artifacts
web/node_modules/
# Any node_modules, wherever a stray `npx` run creates one — the root has no
# package.json, so one appearing there is always an accident.
node_modules/
web/dist/
web/.env
web/.env.*
!web/.env.example
!web/.env.production
web/.emulator-data/
web/*-debug.log
web/.firebase/
.worktrees/notes-visual-foundation/
/.worktrees/today-planner-ui
